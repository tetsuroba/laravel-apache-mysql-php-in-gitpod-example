image:
  file: Dockerfile
ports:
  - port: 8001
    onOpen: ignore
tasks:
  - init: composer install
          composer update
  - name: Apache
    command: >
        apachectl start;
        multitail /var/log/apache2/error.log -I /var/log/apache2/access.log